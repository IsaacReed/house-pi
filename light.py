import RPi.GPIO as GPIO
GPIO.setmode(GPIO.BCM)
GPIO.setwarning(false)

class Pi:
    def --init--(self):
        self.mode=GPIO.BCM
        self.warning=false

pi = Pi()
pi.mode=("light")
pi.warning=false
